{
  "name": "terraform-workspace",
  "displayName": "Terraform Workspace",
  "description": "AI-powered IaC workspace: Terraform via GitHub Actions, plus AWS CodeBuild executors, Lambda container images, and Service Catalog product authoring.",
  "version": "0.3.0",
  "publisher": "HappyPathway",
  "license": "MIT",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/djaboxx/vscode-terraform-workspace"
  },
  "bugs": {
    "url": "https://github.com/djaboxx/vscode-terraform-workspace/issues"
  },
  "homepage": "https://github.com/djaboxx/vscode-terraform-workspace#readme",
  "qna": "https://github.com/djaboxx/vscode-terraform-workspace/discussions",
  "galleryBanner": {
    "color": "#1e293b",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other",
    "AI",
    "Programming Languages"
  ],
  "keywords": [
    "terraform",
    "opentofu",
    "iac",
    "github-actions",
    "ai",
    "copilot",
    "aws",
    "codebuild",
    "lambda",
    "service-catalog"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "virtualWorkspaces": {
      "supported": "limited",
      "description": "Workspace-config editing and chat work in virtual workspaces, but `terraform`, `aws`, and the workflow scaffolders need a real filesystem and CLI tooling."
    },
    "untrustedWorkspaces": {
      "supported": false,
      "description": "Generates and writes GitHub Actions workflows + Terraform modules and shells out to terraform/aws/git, so it requires a trusted workspace."
    }
  },
  "contributes": {
    "jsonValidation": [
      {
        "fileMatch": ".vscode/terraform-workspace.json",
        "url": "./schemas/terraform-workspace.schema.json"
      }
    ],
    "chatParticipants": [
      {
        "id": "terraform.assistant",
        "fullName": "Terraform Workspace",
        "name": "terraform",
        "description": "AI-powered Terraform workspace manager. Generate code, run plans/applies via GitHub Actions, manage secrets and environments.",
        "isSticky": true,
        "commands": [
          {
            "name": "generate",
            "description": "Generate new Terraform HCL from a description"
          },
          {
            "name": "modify",
            "description": "Modify existing Terraform files via diff preview"
          },
          {
            "name": "plan",
            "description": "Trigger a Terraform plan via GitHub Actions"
          },
          {
            "name": "apply",
            "description": "Trigger a Terraform apply via GitHub Actions"
          },
          {
            "name": "explain",
            "description": "Explain the current Terraform configuration"
          },
          {
            "name": "workspace",
            "description": "List or select Terraform workspaces (GitHub Environments)"
          },
          {
            "name": "bootstrap",
            "description": "Bootstrap a new repository with Terraform workspace patterns"
          },
          {
            "name": "varset",
            "description": "Manage variable sets for an organization"
          },
          {
            "name": "search",
            "description": "Search GitHub org repos for Terraform code examples and documentation"
          }
        ]
      },
      {
        "id": "terraform.dave",
        "fullName": "Dave",
        "name": "dave",
        "description": "Everything @terraform does, but Dave does it better — and he knows it.",
        "isSticky": true,
        "commands": [
          {
            "name": "generate",
            "description": "Generate new Terraform HCL from a description"
          },
          {
            "name": "modify",
            "description": "Modify existing Terraform files via diff preview"
          },
          {
            "name": "plan",
            "description": "Trigger a Terraform plan via GitHub Actions"
          },
          {
            "name": "apply",
            "description": "Trigger a Terraform apply via GitHub Actions"
          },
          {
            "name": "explain",
            "description": "Explain the current Terraform configuration"
          },
          {
            "name": "workspace",
            "description": "List or select Terraform workspaces (GitHub Environments)"
          },
          {
            "name": "bootstrap",
            "description": "Bootstrap a new repository with Terraform workspace patterns"
          },
          {
            "name": "varset",
            "description": "Manage variable sets for an organization"
          },
          {
            "name": "search",
            "description": "Search GitHub org repos for Terraform code examples"
          }
        ]
      }
    ],
    "languageModelTools": [
      {
        "name": "terraform_run_plan",
        "displayName": "Run Terraform Plan",
        "modelDescription": "Triggers a terraform plan via GitHub Actions workflow_dispatch on the specified workspace (GitHub Environment). Returns the run URL and streams logs.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "workspace": {
              "type": "string",
              "description": "The GitHub Environment name (Terraform workspace) to plan. Defaults to current active workspace."
            },
            "workingDirectory": {
              "type": "string",
              "description": "The working directory within the repo containing the Terraform configuration."
            }
          }
        }
      },
      {
        "name": "terraform_run_apply",
        "displayName": "Run Terraform Apply",
        "modelDescription": "Triggers a terraform apply via GitHub Actions on the specified workspace. Requires confirmation. Returns the run URL.",
        "inputSchema": {
          "type": "object",
          "required": [
            "workspace"
          ],
          "properties": {
            "workspace": {
              "type": "string",
              "description": "The GitHub Environment name to apply."
            },
            "workingDirectory": {
              "type": "string"
            }
          }
        }
      },
      {
        "name": "terraform_get_state",
        "displayName": "Get Terraform State",
        "modelDescription": "Fetches and parses the Terraform state from the last successful run artifact. Returns a resource summary.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "workspace": {
              "type": "string"
            }
          }
        }
      },
      {
        "name": "terraform_list_workspaces",
        "displayName": "List Terraform Workspaces",
        "modelDescription": "Lists all GitHub Environments configured for the current repository, which correspond to Terraform workspaces.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "terraform_list_variables",
        "displayName": "List Terraform Variables",
        "modelDescription": "Lists all GitHub Actions variables and secrets (non-sensitive names only) for the current workspace or organization.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "workspace": {
              "type": "string"
            },
            "scope": {
              "type": "string",
              "enum": [
                "environment",
                "repository",
                "organization"
              ],
              "description": "Scope to list variables from."
            }
          }
        }
      },
      {
        "name": "terraform_set_variable",
        "displayName": "Set Terraform Variable",
        "modelDescription": "Sets a GitHub Actions variable or secret in the specified scope. Sensitive values are stored as encrypted secrets.",
        "inputSchema": {
          "type": "object",
          "required": [
            "key",
            "value",
            "sensitive"
          ],
          "properties": {
            "key": {
              "type": "string"
            },
            "value": {
              "type": "string"
            },
            "sensitive": {
              "type": "boolean",
              "description": "If true, stored as a GitHub Secret. If false, stored as a GitHub Variable."
            },
            "category": {
              "type": "string",
              "enum": [
                "terraform",
                "env"
              ],
              "description": "Whether this is a TF_VAR_ or plain env var."
            },
            "workspace": {
              "type": "string"
            },
            "scope": {
              "type": "string",
              "enum": [
                "environment",
                "repository",
                "organization"
              ]
            }
          }
        }
      },
      {
        "name": "terraform_generate_code",
        "displayName": "Generate Terraform Code",
        "modelDescription": "Generates Terraform HCL from a natural language description, using detected provider versions and HappyPathway module patterns as context.",
        "inputSchema": {
          "type": "object",
          "required": [
            "description"
          ],
          "properties": {
            "description": {
              "type": "string",
              "description": "What infrastructure to create or modify."
            },
            "targetFile": {
              "type": "string",
              "description": "Target .tf file path. If omitted, creates a new file."
            },
            "useModules": {
              "type": "boolean",
              "description": "Prefer HappyPathway Terraform modules from the registry."
            }
          }
        }
      },
      {
        "name": "terraform_bootstrap_workspace",
        "displayName": "Bootstrap Terraform Workspace",
        "modelDescription": "Sets up a GitHub repository with Terraform environments, GitHub Actions workflows, branch protection, and S3 backend config using HappyPathway patterns.",
        "inputSchema": {
          "type": "object",
          "required": [
            "repoName",
            "repoOrg",
            "environments"
          ],
          "properties": {
            "repoName": {
              "type": "string"
            },
            "repoOrg": {
              "type": "string"
            },
            "environments": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string"
                  },
                  "branch": {
                    "type": "string"
                  },
                  "enforceReviewers": {
                    "type": "boolean"
                  },
                  "reviewerTeams": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  },
                  "cacheBucket": {
                    "type": "string"
                  }
                }
              }
            },
            "stateBucket": {
              "type": "string"
            },
            "stateRegion": {
              "type": "string"
            }
          }
        }
      },
      {
        "name": "terraform_read_config",
        "displayName": "Read Terraform Workspace Config",
        "modelDescription": "Returns the full contents of .vscode/terraform-workspace.json for the active workspace folder as JSON. Use this before making config changes so you know the current state.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "terraform_update_config",
        "displayName": "Update Terraform Workspace Config",
        "modelDescription": "Merges a partial patch into .vscode/terraform-workspace.json. Supports updating stateConfig (bucket, region, keyPrefix, dynamodbTable), compositeActions (action refs), compositeActionOrg, and repo metadata. Use terraform_read_config first to see current values.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "stateConfig": {
              "type": "object",
              "description": "Partial state backend config to merge in.",
              "properties": {
                "bucket": {
                  "type": "string"
                },
                "region": {
                  "type": "string"
                },
                "keyPrefix": {
                  "type": "string"
                },
                "dynamodbTable": {
                  "type": "string"
                },
                "setBackend": {
                  "type": "boolean"
                }
              }
            },
            "compositeActions": {
              "type": "object",
              "description": "Partial composite action refs to merge in.",
              "properties": {
                "checkout": {
                  "type": "string"
                },
                "awsAuth": {
                  "type": "string"
                },
                "ghAuth": {
                  "type": "string"
                },
                "setupTerraform": {
                  "type": "string"
                },
                "terraformInit": {
                  "type": "string"
                },
                "terraformPlan": {
                  "type": "string"
                },
                "terraformApply": {
                  "type": "string"
                },
                "s3Cleanup": {
                  "type": "string"
                }
              }
            },
            "compositeActionOrg": {
              "type": "string",
              "description": "GitHub org that owns the composite action repos."
            },
            "repo": {
              "type": "object",
              "description": "Repo metadata fields to update.",
              "properties": {
                "name": {
                  "type": "string"
                },
                "repoOrg": {
                  "type": "string"
                },
                "description": {
                  "type": "string"
                },
                "isPrivate": {
                  "type": "boolean"
                },
                "enforcePrs": {
                  "type": "boolean"
                },
                "createCodeowners": {
                  "type": "boolean"
                },
                "codeownersTeam": {
                  "type": "string"
                },
                "adminTeams": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "repoTopics": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      },
      {
        "name": "terraform_sync_workflows",
        "displayName": "Sync Terraform Workflows",
        "modelDescription": "Generates GitHub Actions plan and apply workflow YAML files for every environment in the workspace config and writes them to .github/workflows/. Fetches live variable and secret names from GitHub to ensure every secret is explicitly referenced. Run this after terraform_update_config or whenever environments change.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "terraform_get_run_status",
        "displayName": "Get Terraform Run Status",
        "modelDescription": "Returns the most recent plan and apply workflow run statuses (status, conclusion, URL) for one or all configured workspaces. Use to check whether infra is in sync or a run is in progress.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "workspace": {
              "type": "string",
              "description": "Specific workspace (GitHub Environment) name. Omit to check all environments."
            },
            "limit": {
              "type": "number",
              "description": "Max runs to return per workflow. Default 3."
            }
          }
        }
      },
      {
        "name": "terraform_search_tf_code",
        "displayName": "Search Terraform Code",
        "modelDescription": "Searches the current GitHub org's repositories for Terraform code matching a query. Returns file paths, repo names, and code snippets. Use to find examples, check how a resource is used across the org, or discover existing module usage.",
        "inputSchema": {
          "type": "object",
          "required": [
            "query"
          ],
          "properties": {
            "query": {
              "type": "string",
              "description": "Free-text search query (e.g. 'aws_s3_bucket versioning', 'google_storage_bucket', 'module vpc')."
            },
            "limit": {
              "type": "number",
              "description": "Max results to return (default 10, max 30)."
            }
          }
        }
      },
      {
        "name": "terraform_discover_workspace",
        "displayName": "Discover Terraform Workspace Defaults",
        "modelDescription": "Auto-discovers reasonable defaults for the workspace config by scanning git remotes, .tf files (S3 backend, providers, regions, required_version), .tfvars files, .github/workflows YAML, and existing GitHub Environments. Returns a discovery summary AND a fully-populated suggested .vscode/terraform-workspace.json. Use this before asking the user for config values — most fields can be inferred. If `applyDefaults=true`, writes the config to disk; otherwise just returns it for review.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "subdirectory": {
              "type": "string",
              "description": "Optional subdirectory of the workspace folder to scan."
            },
            "applyDefaults": {
              "type": "boolean",
              "description": "If true, writes the suggested config to .vscode/terraform-workspace.json after discovery. Defaults to false."
            }
          }
        }
      },
      {
        "name": "terraform_delete_variable",
        "displayName": "Delete Variable or Secret",
        "modelDescription": "Deletes a GitHub Actions variable or secret from the active repository or one of its environments. Set `sensitive=true` for secrets. For environment scope, supply `environment`. Requires user confirmation.",
        "inputSchema": {
          "type": "object",
          "required": [
            "scope",
            "key"
          ],
          "properties": {
            "scope": {
              "type": "string",
              "enum": [
                "environment",
                "repository"
              ],
              "description": "Where the variable lives."
            },
            "key": {
              "type": "string",
              "description": "Variable/secret name (env-var-style identifier)."
            },
            "environment": {
              "type": "string",
              "description": "Required when scope is 'environment'."
            },
            "sensitive": {
              "type": "boolean",
              "description": "True for secrets, false (default) for variables."
            }
          }
        }
      },
      {
        "name": "terraform_resolve_variable",
        "displayName": "Resolve Variable Source",
        "modelDescription": "Traces where a GitHub Actions variable is defined across org/repo/environment scopes for the active workspace and reports the effective (most specific) source. Use to debug 'why does this var have that value?' questions.",
        "inputSchema": {
          "type": "object",
          "required": [
            "key"
          ],
          "properties": {
            "key": {
              "type": "string",
              "description": "Variable name to trace."
            },
            "environment": {
              "type": "string",
              "description": "Optional environment to include in the lookup."
            }
          }
        }
      },
      {
        "name": "terraform_review_deployment",
        "displayName": "Approve or Reject Deployment",
        "modelDescription": "Approves or rejects pending GitHub Actions deployment(s) gated by environment protection rules. Pass `runId` from a workflow run, `state` ('approved' or 'rejected'), an optional list of `environments` to filter to, and an optional `comment`. Requires user confirmation.",
        "inputSchema": {
          "type": "object",
          "required": [
            "runId",
            "state"
          ],
          "properties": {
            "runId": {
              "type": "number",
              "description": "GitHub Actions workflow run ID."
            },
            "state": {
              "type": "string",
              "enum": [
                "approved",
                "rejected"
              ],
              "description": "Decision."
            },
            "environments": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional list of environment names to act on. Defaults to all approvable."
            },
            "comment": {
              "type": "string",
              "description": "Optional audit-log comment."
            }
          }
        }
      },
      {
        "name": "terraform_lint_workflows",
        "displayName": "Lint GitHub Workflows",
        "modelDescription": "Runs `actionlint` over the active folder's `.github/workflows/*.{yml,yaml}` and returns a structured list of issues (file, line, kind, message). Requires `actionlint` on PATH.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "terraform_check_drift",
        "displayName": "Check Terraform Drift",
        "modelDescription": "Inspects the latest plan workflow run for each configured environment and returns environments whose plan exited with code 2 (pending changes / drift). Use to find environments that need an apply.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "terraform_scaffold_backend",
        "displayName": "Scaffold S3 Terraform Backend",
        "modelDescription": "Generates Terraform code for an S3 state bucket + DynamoDB lock table (the bootstrap a team writes once per AWS account). Returns the HCL — does not apply it. Use to bootstrap remote state for a new account.",
        "inputSchema": {
          "type": "object",
          "required": [
            "bucketName",
            "region",
            "dynamodbTable"
          ],
          "properties": {
            "bucketName": {
              "type": "string",
              "description": "Globally-unique S3 bucket name."
            },
            "region": {
              "type": "string",
              "description": "AWS region (e.g. us-east-1)."
            },
            "dynamodbTable": {
              "type": "string",
              "description": "Name for the DynamoDB lock table."
            },
            "kmsKeyAlias": {
              "type": "string",
              "description": "Optional KMS key alias for SSE-KMS encryption (without 'alias/' prefix). Defaults to AES256."
            }
          }
        }
      },
      {
        "name": "terraform_scaffold_oidc_trust",
        "displayName": "Scaffold GitHub OIDC Trust Policy",
        "modelDescription": "Generates an AWS IAM trust policy JSON allowing GitHub Actions in a specific org/repo/environment to AssumeRoleWithWebIdentity via OIDC. Returns the JSON — does not apply it.",
        "inputSchema": {
          "type": "object",
          "required": [
            "awsAccountId",
            "githubOrg"
          ],
          "properties": {
            "awsAccountId": {
              "type": "string",
              "description": "12-digit AWS account ID."
            },
            "githubOrg": {
              "type": "string",
              "description": "GitHub org/owner that will assume the role."
            },
            "repo": {
              "type": "string",
              "description": "Optional repo name; omit to allow any repo in the org."
            },
            "environment": {
              "type": "string",
              "description": "Optional environment name; omit to allow any environment."
            },
            "oidcProvider": {
              "type": "string",
              "description": "Override the default OIDC provider host (defaults to token.actions.githubusercontent.com)."
            }
          }
        }
      },
      {
        "name": "terraform_scaffold_from_template",
        "displayName": "Scaffold Repo From Template",
        "modelDescription": "Creates a new GitHub repository from a template repository via POST /repos/{template_owner}/{template_repo}/generate. Use this for the 'start a new module / service from a template' workflow. Returns the new repo's URL and clone URL. Requires the 'repo' scope on the target owner.",
        "inputSchema": {
          "type": "object",
          "required": [
            "templateOwner",
            "templateRepo",
            "newRepoName"
          ],
          "properties": {
            "templateOwner": {
              "type": "string",
              "description": "Owner of the template repository (user or org)."
            },
            "templateRepo": {
              "type": "string",
              "description": "Name of the template repository. Must be marked as a template on GitHub."
            },
            "newRepoName": {
              "type": "string",
              "description": "Name for the new repository.",
              "pattern": "^[a-zA-Z0-9._-]+$"
            },
            "newRepoOwner": {
              "type": "string",
              "description": "Owner for the new repo. Defaults to the authenticated user; supply an org name to create under an org."
            },
            "description": {
              "type": "string",
              "description": "Optional description for the new repository."
            },
            "privateRepo": {
              "type": "boolean",
              "description": "Create as private. Default: true."
            },
            "includeAllBranches": {
              "type": "boolean",
              "description": "Include all branches from the template, not just the default branch. Default: false."
            }
          }
        }
      },
      {
        "name": "terraform_scaffold_module_repo",
        "displayName": "Scaffold Terraform Module Repo",
        "modelDescription": "Generates the standard Terraform *module* repo skeleton (USAGE.md Pattern 1) into the active workspace folder: main.tf, variables.tf, outputs.tf, versions.tf, examples/<name>/, README.md with terraform-docs markers, .gitignore, and .terraform-docs.yml. Use this when starting a new reusable module from scratch in an empty repo. For creating a GitHub repo from an existing template, use terraform_scaffold_from_template instead. Existing files are skipped unless 'overwrite' is true.",
        "inputSchema": {
          "type": "object",
          "required": [
            "moduleName",
            "provider"
          ],
          "properties": {
            "moduleName": {
              "type": "string",
              "description": "Module repo name, e.g. 'terraform-aws-vpc'. Drives README headings and the local module name in examples.",
              "pattern": "^[a-zA-Z0-9._-]+$"
            },
            "provider": {
              "type": "string",
              "description": "Primary provider key, e.g. 'aws', 'azurerm', 'google', 'kubernetes'. Drives versions.tf required_providers.",
              "pattern": "^[a-z][a-z0-9_-]*$"
            },
            "description": {
              "type": "string",
              "description": "Optional one-line description used in the README tagline."
            },
            "exampleNames": {
              "type": "array",
              "description": "Folder names under examples/ to scaffold. Defaults to ['basic'].",
              "items": {
                "type": "string",
                "pattern": "^[a-zA-Z0-9._-]+$"
              }
            },
            "includeDevcontainer": {
              "type": "boolean",
              "description": "Also write .devcontainer/devcontainer.json for VS Code Dev Containers. Default: false."
            },
            "requiredVersion": {
              "type": "string",
              "description": "Terraform/OpenTofu required_version constraint. Default: '>= 1.5.0'."
            },
            "targetDirectory": {
              "type": "string",
              "description": "Subdirectory of the active workspace folder to scaffold into. Defaults to the workspace root. Path traversal ('..') is rejected."
            },
            "overwrite": {
              "type": "boolean",
              "description": "If true, replace existing files. If false (default), existing files are reported as skipped."
            }
          }
        }
      },
      {
        "name": "terraform_lookup_provider_doc",
        "displayName": "Lookup Provider Doc (version-pinned)",
        "modelDescription": "Returns the official documentation page for a Terraform provider resource or data source at the EXACT version pinned in the workspace's `.terraform.lock.hcl`. Run `terraform init` first so the lock file exists. Docs are fetched from the public Terraform Registry on first use and cached locally per version. Use this instead of guessing argument names — it guarantees parity with the resolved provider version (which may lag behind `latest`). Provider must be passed as `<namespace>/<name>` (e.g. `hashicorp/aws`). Resource may be passed with or without the provider prefix (`s3_bucket` and `aws_s3_bucket` both work).",
        "inputSchema": {
          "type": "object",
          "required": [
            "provider",
            "resource"
          ],
          "properties": {
            "provider": {
              "type": "string",
              "description": "Provider source as `<namespace>/<name>`, e.g. `hashicorp/aws`, `hashicorp/google`, `integrations/github`."
            },
            "resource": {
              "type": "string",
              "description": "Resource or data-source slug, e.g. `s3_bucket` or `aws_s3_bucket`."
            },
            "category": {
              "type": "string",
              "enum": [
                "resources",
                "data-sources",
                "guides",
                "functions",
                "overview"
              ],
              "description": "Doc category. Default `resources`."
            },
            "maxChars": {
              "type": "number",
              "description": "Truncate the returned markdown to this many characters. Default 8000.",
              "minimum": 200,
              "maximum": 50000
            }
          }
        }
      },
      {
        "name": "terraform_scaffold_codebuild_executor",
        "displayName": "Scaffold CodeBuild Executor (Terraform module)",
        "modelDescription": "Generates a Terraform module under `infra/codebuild-executor-<projectName>/` that provisions an AWS CodeBuild project + S3 source bucket + IAM role used as the dispatched terraform executor (same shape as djaboxx/packer-pipeline). After the user runs `terraform apply` on the module, they should set `executor: 'codebuild'` and `codebuild: { project, sourceBucket, region }` in `.vscode/terraform-workspace.json` so generated workflows (and the local `terraform_dispatch_codebuild_run` tool) use it. Use this when the user is on GHE Server, has no OIDC, or otherwise wants the heavy terraform work to run in CodeBuild rather than on the GHA runner.",
        "inputSchema": {
          "type": "object",
          "required": [
            "region",
            "repoFullName",
            "projectName",
            "sourceBucketName"
          ],
          "properties": {
            "region": {
              "type": "string",
              "description": "AWS region the project lives in (e.g. us-east-1, us-gov-west-1).",
              "pattern": "^[a-z]{2}-[a-z]+-\\d+$"
            },
            "repoFullName": {
              "type": "string",
              "description": "GitHub <owner>/<repo>.",
              "pattern": "^[^/\\s]+/[^/\\s]+$"
            },
            "projectName": {
              "type": "string",
              "description": "CodeBuild project name; also used as the IAM role prefix.",
              "pattern": "^[A-Za-z0-9._-]+$",
              "minLength": 2,
              "maxLength": 150
            },
            "sourceBucketName": {
              "type": "string",
              "description": "Name of the S3 bucket the module creates (used for source uploads + plan artifacts).",
              "pattern": "^[a-z0-9.-]{3,63}$"
            },
            "image": {
              "type": "string",
              "description": "Optional CodeBuild image. Default aws/codebuild/amazonlinux2-x86_64-standard:5.0."
            },
            "computeType": {
              "type": "string",
              "enum": [
                "BUILD_GENERAL1_SMALL",
                "BUILD_GENERAL1_MEDIUM",
                "BUILD_GENERAL1_LARGE",
                "BUILD_GENERAL1_2XLARGE"
              ],
              "description": "Optional compute type. Default BUILD_GENERAL1_MEDIUM."
            },
            "terraformVersion": {
              "type": "string",
              "description": "Terraform version the buildspec installs. Default 1.9.5."
            },
            "stateBucketArns": {
              "type": "array",
              "items": {
                "type": "string",
                "pattern": "^arn:aws[a-z-]*:s3:::[^/]+$"
              },
              "description": "Optional state bucket ARN(s) the executor role needs read/write on."
            },
            "lockTableArn": {
              "type": "string",
              "pattern": "^arn:aws[a-z-]*:dynamodb:",
              "description": "Optional DynamoDB lock table ARN."
            },
            "extraManagedPolicyArns": {
              "type": "array",
              "items": {
                "type": "string",
                "pattern": "^arn:aws[a-z-]*:iam::"
              },
              "description": "Optional managed-policy ARNs to attach (e.g. AdministratorAccess for sandbox)."
            }
          }
        }
      },
      {
        "name": "terraform_dispatch_codebuild_run",
        "displayName": "Dispatch terraform plan/apply into CodeBuild",
        "modelDescription": "Triggers a `terraform plan` or `terraform apply` for the named workspace inside the CodeBuild project configured under `codebuild` in `.vscode/terraform-workspace.json`. Same flow as the generated GHA workflow: zips the working directory, uploads to S3, calls codebuild:StartBuild, tails CloudWatch logs into the output channel, downloads plan artifacts back. Requires AWS credentials in the user's environment (e.g. via `awscreds`). For `apply`, prompts the user for confirmation before running.",
        "inputSchema": {
          "type": "object",
          "required": [
            "command",
            "workspace"
          ],
          "properties": {
            "command": {
              "type": "string",
              "enum": [
                "plan",
                "apply"
              ],
              "description": "Which terraform subcommand to dispatch."
            },
            "workspace": {
              "type": "string",
              "description": "Workspace/environment name to run against (must exist in workspace config)."
            }
          }
        }
      },
      {
        "name": "terraform_scaffold_lambda_image",
        "displayName": "Scaffold Lambda container image pipeline",
        "modelDescription": "Generates `infra/lambda-image-<functionName>/` with a packer.pkr.hcl + build.hcl (consumed by djaboxx/packer-pipeline) + ECR Terraform + Lambda+IAM Terraform. Uses partition-aware ARNs (data.aws_partition.current) so the output works in GovCloud (arn:aws-us-gov:*) and commercial. Supports optional VPC config (subnet + SG lookup by name tag), an optional CodeBuild project resource + StartBuild/BatchGetBuilds IAM policy (for long-running Lambda→CodeBuild patterns), and an optional Secrets Manager policy for GitHub token access. Also writes `src/handler.py` + `src/requirements.txt` skeletons (only if missing).",
        "inputSchema": {
          "type": "object",
          "required": [
            "functionName",
            "region",
            "packerSourceBucket",
            "packerCodebuildProject"
          ],
          "properties": {
            "functionName": {
              "type": "string",
              "pattern": "^[A-Za-z0-9_-]{1,64}$",
              "description": "Logical function name (used for the dir, ECR repo, and Lambda name)."
            },
            "region": {
              "type": "string",
              "pattern": "^[a-z]{2}(-gov)?-[a-z]+-\\d+$"
            },
            "packerSourceBucket": {
              "type": "string",
              "pattern": "^[a-z0-9.-]{3,63}$",
              "description": "S3 bucket the existing packer-pipeline CodeBuild project reads sources from."
            },
            "packerCodebuildProject": {
              "type": "string",
              "minLength": 2,
              "maxLength": 150,
              "description": "Name of the existing packer-pipeline CodeBuild project."
            },
            "baseImage": {
              "type": "string",
              "description": "Base image (default public.ecr.aws/lambda/python:3.12)."
            },
            "handler": {
              "type": "string",
              "pattern": "^[A-Za-z_][A-Za-z0-9_]*\\.[A-Za-z_][A-Za-z0-9_]*$",
              "description": "Handler entrypoint (default handler.lambda_handler)."
            },
            "memorySize": {
              "type": "number",
              "minimum": 128,
              "maximum": 10240
            },
            "timeout": {
              "type": "number",
              "minimum": 1,
              "maximum": 900
            },
            "envVars": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            "extraManagedPolicyArns": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Extra managed-policy ARNs to attach. Use partition-aware ARNs (arn:aws-us-gov:iam::aws:policy/...) for GovCloud."
            },
            "subnetNameTag": {
              "type": "string",
              "description": "Name tag of the VPC subnet for Lambda VPC config. When set, also renders data.aws_subnet + vpc_config. Required for GHE/private-network access."
            },
            "securityGroupName": {
              "type": "string",
              "description": "Security group GroupName to look up when VPC is enabled (subnetNameTag is set)."
            },
            "codebuildProjectName": {
              "type": "string",
              "description": "When set, generates an aws_codebuild_project + IAM StartBuild/BatchGetBuilds policy. Use this when the Lambda's job is to trigger CodeBuild for long-running work."
            },
            "codebuildRoleName": {
              "type": "string",
              "description": "Name of an existing IAM role for the CodeBuild project. When omitted, a minimal role is created."
            },
            "githubTokenSecretName": {
              "type": "string",
              "description": "Secrets Manager secret name for a GitHub token. When set, adds a GetSecretValue IAM policy on the Lambda exec role."
            }
          }
        }
      },
      {
        "name": "terraform_build_lambda_image",
        "displayName": "Build & publish Lambda image (via packer-pipeline CodeBuild)",
        "modelDescription": "Zips the lambda image directory, uploads to S3, dispatches the existing packer-pipeline CodeBuild project, tails CloudWatch logs, then resolves the resulting ECR image digest and writes it to `terraform.tfvars.json` next to lambda.tf so the next `terraform apply` deploys the function pinned by digest (never tag).",
        "inputSchema": {
          "type": "object",
          "required": [
            "functionName",
            "region",
            "packerSourceBucket",
            "packerCodebuildProject",
            "ecrRepoName",
            "directory"
          ],
          "properties": {
            "functionName": {
              "type": "string",
              "pattern": "^[A-Za-z0-9_-]{1,64}$"
            },
            "region": {
              "type": "string",
              "pattern": "^[a-z]{2}-[a-z]+-\\d+$"
            },
            "packerSourceBucket": {
              "type": "string",
              "pattern": "^[a-z0-9.-]{3,63}$"
            },
            "packerCodebuildProject": {
              "type": "string",
              "minLength": 2,
              "maxLength": 150
            },
            "ecrRepoName": {
              "type": "string",
              "pattern": "^[a-z0-9._/-]{2,256}$",
              "description": "ECR repository name (NOT the URI)."
            },
            "imageTag": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "description": "Tag to push (default `build-<timestamp>`). Digest is captured separately."
            },
            "directory": {
              "type": "string",
              "minLength": 1,
              "description": "Workspace-relative path to the lambda image dir (e.g. infra/lambda-image-myfn)."
            }
          }
        }
      },
      {
        "name": "terraform_scaffold_sc_product",
        "displayName": "Scaffold Service Catalog product + portfolio + S3 bucket + launch role + constraint",
        "modelDescription": "Generates `infra/sc-product-<slug>/product.tf` that creates: a dedicated S3 bucket for the CFN template (public-read, required for GovCloud launch-role access), the template as an `aws_s3_object`, an `aws_servicecatalog_portfolio`, an `aws_servicecatalog_product` with an initial dummy artifact, a separate `aws_servicecatalog_provisioning_artifact` (the real working artifact), a `null_resource` to deprecate the dummy via local-exec, an IAM launch role with CFN + s3:* + optional Lambda InvokeFunction, and a LAUNCH constraint. Place your product-template.yaml alongside product.tf before running `terraform apply`. Works correctly in GovCloud.",
        "inputSchema": {
          "type": "object",
          "required": [
            "productSlug",
            "portfolioName",
            "owner",
            "templateKey",
            "region"
          ],
          "properties": {
            "productSlug": {
              "type": "string",
              "pattern": "^[a-z0-9-]{1,60}$",
              "description": "Short slug used for resource names, directory, and S3 bucket name (lowercase, hyphens OK)."
            },
            "portfolioName": {
              "type": "string",
              "description": "Human-readable portfolio name shown in SC console."
            },
            "portfolioDescription": {
              "type": "string"
            },
            "owner": {
              "type": "string"
            },
            "templateKey": {
              "type": "string",
              "minLength": 1,
              "description": "S3 key for the CFN template, e.g. '2-0-0.yaml'."
            },
            "region": {
              "type": "string",
              "pattern": "^[a-z]{2}(-gov)?-[a-z]+-\\d+$"
            },
            "description": {
              "type": "string"
            },
            "initialVersion": {
              "type": "string",
              "pattern": "^\\d+\\.\\d+\\.\\d+$",
              "description": "Initial provisioning artifact version (default 1.0.0)."
            },
            "lambdaArn": {
              "type": "string",
              "description": "Full Lambda ARN (including account ID) that the CFN Custom Resource invokes. Added to the launch role's InvokeFunction policy."
            },
            "principalArns": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "IAM principal ARNs or IAM_PATTERN globs granted portfolio access."
            },
            "existingLaunchRoleName": {
              "type": "string",
              "description": "When set, looks up this existing IAM role instead of creating a new one."
            }
          }
        }
      },
      {
        "name": "terraform_bump_sc_artifact",
        "displayName": "Bump Service Catalog provisioning artifact",
        "modelDescription": "Writes an additive Terraform file (`artifact-v<version>.tf`) into an existing SC product directory that adds a new `aws_servicecatalog_provisioning_artifact` with active=true and guidance=DEFAULT, plus a `null_resource` that automatically deprecates the previous DEFAULT artifact via `aws servicecatalog update-provisioning-artifact`. Uses a region-qualified S3 URL that works in both GovCloud and commercial. Use this for the day-to-day 'I changed the form, ship a new version' flow.",
        "inputSchema": {
          "type": "object",
          "required": [
            "productResourceName",
            "newVersion",
            "templateBucket",
            "templateKey",
            "directory"
          ],
          "properties": {
            "productResourceName": {
              "type": "string",
              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
              "description": "Terraform resource name (e.g. `this`) of the existing aws_servicecatalog_product."
            },
            "newVersion": {
              "type": "string",
              "pattern": "^\\d+\\.\\d+\\.\\d+$"
            },
            "templateBucket": {
              "type": "string",
              "pattern": "^[a-z0-9.-]{3,63}$"
            },
            "templateKey": {
              "type": "string",
              "minLength": 1,
              "description": "S3 key to the new template artifact."
            },
            "description": {
              "type": "string"
            },
            "directory": {
              "type": "string",
              "minLength": 1,
              "description": "Workspace-relative dir containing the existing product.tf."
            }
          }
        }
      },
      {
        "name": "terraform_dry_render_sc_product",
        "displayName": "Dry-render Service Catalog form inputs against schema",
        "modelDescription": "Validates a sample SC form inputs object against a JSON schema (the same shape the SC dispatcher Lambda receives). Pure local validation — no AWS calls. Catches missing required fields, enum mismatches, regex failures, length/range violations. Use this to verify a form change before users encounter it in the SC console.",
        "inputSchema": {
          "type": "object",
          "required": [
            "schema",
            "sample"
          ],
          "properties": {
            "schema": {
              "type": "object",
              "description": "JSON schema describing the SC form's input parameters."
            },
            "sample": {
              "type": "object",
              "description": "Sample inputs to validate."
            }
          }
        }
      },
      {
        "name": "terraform_scaffold_python_dev_env",
        "displayName": "Scaffold Python developer inner-loop for a Lambda image dir",
        "modelDescription": "Layers a Python developer environment onto an existing infra/lambda-image-<fn>/ directory: pyproject.toml (pinned to the base-image Python version with pytest+ruff+mypy+moto+pip-tools dev deps), Makefile (install/test/lint/typecheck/freeze), .python-version, .devcontainer/devcontainer.json (no Docker — Codespaces-friendly), .vscode/launch.json (Local invoke + pytest debug configs), tests/conftest.py with a lambda_context fixture, tests/test_handler.py smoke test, tests/events/sample.json, and scripts/local_invoke.py (stdlib-only driver). Existing files are skipped, never overwritten. Fully Docker-free.",
        "inputSchema": {
          "type": "object",
          "required": [
            "directory",
            "functionName",
            "pythonVersion",
            "handler"
          ],
          "properties": {
            "directory": {
              "type": "string",
              "minLength": 1,
              "description": "Workspace-relative path to the lambda image dir."
            },
            "functionName": {
              "type": "string",
              "pattern": "^[A-Za-z0-9_-]{1,64}$"
            },
            "pythonVersion": {
              "type": "string",
              "pattern": "^3\\.\\d+$",
              "description": "Python version, e.g. \"3.12\"."
            },
            "handler": {
              "type": "string",
              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*(\\.[a-zA-Z_][a-zA-Z0-9_]*)+$",
              "description": "Dotted handler path."
            },
            "region": {
              "type": "string",
              "pattern": "^[a-z]{2}-[a-z]+-\\d+$",
              "description": "Optional AWS region — baked into the devcontainer post-create env."
            }
          }
        }
      },
      {
        "name": "terraform_invoke_lambda_locally",
        "displayName": "Invoke a Lambda handler locally (no Docker)",
        "modelDescription": "Runs a Lambda handler in a plain Python interpreter against a JSON event file. Resolves the interpreter from python.defaultInterpreterPath, then <project>/.venv/bin/python, then python3 on PATH. Streams stdout/stderr into the output channel and returns the exit code. Catches handler logic + missing-dep bugs but does NOT exercise the AL2 Lambda runtime (use Lambda: Build & Publish Image for that). Requires that scripts/local_invoke.py was scaffolded by terraform_scaffold_python_dev_env.",
        "inputSchema": {
          "type": "object",
          "required": [
            "directory",
            "functionName",
            "handler",
            "eventPath"
          ],
          "properties": {
            "directory": {
              "type": "string",
              "minLength": 1,
              "description": "Workspace-relative path to the lambda image dir."
            },
            "functionName": {
              "type": "string",
              "pattern": "^[A-Za-z0-9_-]{1,64}$"
            },
            "handler": {
              "type": "string",
              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*(\\.[a-zA-Z_][a-zA-Z0-9_]*)+$"
            },
            "eventPath": {
              "type": "string",
              "minLength": 1,
              "description": "Path to the event JSON, workspace-relative or absolute."
            },
            "pythonPath": {
              "type": "string",
              "description": "Optional explicit interpreter override."
            }
          }
        }
      },
      {
        "name": "terraform_tail_lambda_logs",
        "displayName": "Tail CloudWatch Logs for a Lambda function",
        "modelDescription": "Streams /aws/lambda/<functionName> via `aws logs tail --follow` into the output channel. Cancellation kills the child process. Useful right after a deploy to watch the next invocation. Requires the AWS CLI on PATH and credentials available (env, profile, awscreds, etc.).",
        "inputSchema": {
          "type": "object",
          "required": [
            "region",
            "functionName"
          ],
          "properties": {
            "region": {
              "type": "string",
              "pattern": "^[a-z]{2}-[a-z]+-\\d+$"
            },
            "functionName": {
              "type": "string",
              "pattern": "^[A-Za-z0-9_-]{1,64}$"
            },
            "filterPattern": {
              "type": "string",
              "description": "Optional CloudWatch filter pattern."
            },
            "sinceMinutes": {
              "type": "integer",
              "minimum": 1,
              "maximum": 1440,
              "description": "Look-back window in minutes (default 5)."
            }
          }
        }
      }
    ],
    "keybindings": [
      {
        "command": "terraform.callNotes.open",
        "key": "ctrl+alt+n",
        "mac": "cmd+alt+n"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "terraform-workspace",
          "title": "Terraform Workspace",
          "icon": "images/terraform-activitybar.svg"
        }
      ]
    },
    "views": {
      "terraform-workspace": [
        {
          "id": "terraform.workspaces",
          "name": "Workspaces",
          "contextualTitle": "Terraform Workspaces"
        },
        {
          "id": "terraform.variables",
          "name": "Variables & Secrets",
          "contextualTitle": "Terraform Variables"
        },
        {
          "id": "terraform.requiredSetup",
          "name": "Required Setup",
          "contextualTitle": "Terraform Required Setup"
        },
        {
          "id": "terraform.runs",
          "name": "Run History",
          "contextualTitle": "Terraform Runs"
        }
      ]
    },
    "commands": [
      {
        "command": "terraform.selectFolder",
        "title": "Select Workspace Folder",
        "icon": "$(folder)",
        "category": "Terraform"
      },
      {
        "command": "terraform.refreshWorkspaces",
        "title": "Refresh Workspaces",
        "icon": "$(refresh)",
        "category": "Terraform"
      },
      {
        "command": "terraform.selectWorkspace",
        "title": "Select Workspace",
        "icon": "$(check)",
        "category": "Terraform"
      },
      {
        "command": "terraform.runPlan",
        "title": "Run Plan",
        "icon": "$(play)",
        "category": "Terraform"
      },
      {
        "command": "terraform.runApply",
        "title": "Run Apply",
        "icon": "$(cloud-upload)",
        "category": "Terraform"
      },
      {
        "command": "terraform.openRunLogs",
        "title": "Open Run Logs",
        "icon": "$(output)",
        "category": "Terraform"
      },
      {
        "command": "terraform.addVariable",
        "title": "Add Variable",
        "icon": "$(add)",
        "category": "Terraform"
      },
      {
        "command": "terraform.addSecret",
        "title": "Add Secret",
        "icon": "$(key)",
        "category": "Terraform"
      },
      {
        "command": "terraform.deleteVariable",
        "title": "Delete Variable",
        "icon": "$(trash)",
        "category": "Terraform"
      },
      {
        "command": "terraform.requiredSetup.setAll",
        "title": "Set All Required Variables & Secrets…",
        "icon": "$(checklist)",
        "category": "Terraform"
      },
      {
        "command": "terraform.requiredSetup.refresh",
        "title": "Refresh Required Setup",
        "icon": "$(refresh)",
        "category": "Terraform"
      },
      {
        "command": "terraform.requiredSetup.set",
        "title": "Set Required Value…",
        "category": "Terraform"
      },
      {
        "command": "terraform.pinTerraformVersion",
        "title": "Pin Terraform Version… (.terraform-version)",
        "icon": "$(versions)",
        "category": "Terraform"
      },
      {
        "command": "terraform.refreshProviderDocs",
        "title": "Refresh Provider Docs (from .terraform.lock.hcl)",
        "icon": "$(cloud-download)",
        "category": "Terraform"
      },
      {
        "command": "terraform.scaffoldCodebuildExecutor",
        "title": "Scaffold Executor Project…",
        "icon": "$(server-process)",
        "category": "CodeBuild"
      },
      {
        "command": "terraform.runPlanInCodeBuild",
        "title": "Run Terraform Plan…",
        "icon": "$(cloud-upload)",
        "category": "CodeBuild"
      },
      {
        "command": "terraform.runApplyInCodeBuild",
        "title": "Run Terraform Apply…",
        "icon": "$(cloud-upload)",
        "category": "CodeBuild"
      },
      {
        "command": "terraform.scaffoldLambdaImage",
        "title": "Scaffold Container Image Project…",
        "icon": "$(symbol-method)",
        "category": "Lambda"
      },
      {
        "command": "terraform.buildLambdaImage",
        "title": "Build & Publish Image…",
        "icon": "$(cloud-upload)",
        "category": "Lambda"
      },
      {
        "command": "terraform.scaffoldServiceCatalogProduct",
        "title": "Scaffold Product…",
        "icon": "$(package)",
        "category": "Service Catalog"
      },
      {
        "command": "terraform.bumpServiceCatalogArtifact",
        "title": "Bump Provisioning Artifact…",
        "icon": "$(versions)",
        "category": "Service Catalog"
      },
      {
        "command": "terraform.dryRenderServiceCatalogProduct",
        "title": "Dry-render Product Form Inputs…",
        "icon": "$(beaker)",
        "category": "Service Catalog"
      },
      {
        "command": "terraform.scaffoldPythonDevEnv",
        "title": "Scaffold Python Dev Environment…",
        "icon": "$(snake)",
        "category": "Lambda"
      },
      {
        "command": "terraform.invokeLambdaLocally",
        "title": "Test Locally…",
        "icon": "$(play)",
        "category": "Lambda"
      },
      {
        "command": "terraform.tailLambdaLogs",
        "title": "Tail Function Logs…",
        "icon": "$(output)",
        "category": "Lambda"
      },
      {
        "command": "terraform.bootstrapWorkspace",
        "title": "Bootstrap New Workspace",
        "icon": "$(wand)",
        "category": "Terraform"
      },
      {
        "command": "terraform.openInGitHub",
        "title": "Open in GitHub",
        "icon": "$(github)",
        "category": "Terraform"
      },
      {
        "command": "terraform.configureGithubApp",
        "title": "Configure GitHub App",
        "icon": "$(settings-gear)",
        "category": "Terraform"
      },
      {
        "command": "terraform.configureWorkspace",
        "title": "Configure Workspace",
        "icon": "$(edit)",
        "category": "Terraform"
      },
      {
        "command": "terraform.syncWorkflows",
        "title": "Sync Workflows",
        "icon": "$(sync)",
        "category": "Terraform"
      },
      {
        "command": "terraform.lintWorkflows",
        "title": "Lint Workflows (actionlint)",
        "icon": "$(check)",
        "category": "Terraform"
      },
      {
        "command": "terraform.checkDrift",
        "title": "Check Drift",
        "icon": "$(diff)",
        "category": "Terraform"
      },
      {
        "command": "terraform.scaffoldBackend",
        "title": "Scaffold S3+DynamoDB Backend",
        "icon": "$(database)",
        "category": "Terraform"
      },
      {
        "command": "terraform.discoverDefaults",
        "title": "Auto-discover Workspace Defaults",
        "icon": "$(sparkle)",
        "category": "Terraform"
      },
      {
        "command": "terraform.scaffoldOidcTrust",
        "title": "Generate OIDC Trust Policy",
        "icon": "$(shield)",
        "category": "Terraform"
      },
      {
        "command": "terraform.scaffoldFromTemplate",
        "title": "Scaffold Repo From Template…",
        "icon": "$(repo-create)",
        "category": "Terraform"
      },
      {
        "command": "terraform.composeModules",
        "title": "Compose Terraform Modules…",
        "icon": "$(combine)",
        "category": "Terraform"
      },
      {
        "command": "terraform.agent.start",
        "title": "Agent: Start Autonomous Loop",
        "icon": "$(robot)",
        "category": "Terraform"
      },
      {
        "command": "terraform.agent.stop",
        "title": "Agent: Stop Autonomous Loop",
        "icon": "$(circle-slash)",
        "category": "Terraform"
      },
      {
        "command": "terraform.agent.runNow",
        "title": "Agent: Run One Tick Now",
        "icon": "$(sync)",
        "category": "Terraform"
      },
      {
        "command": "terraform.agent.showStatus",
        "title": "Agent: Show Output Log",
        "icon": "$(output)",
        "category": "Terraform"
      },
      {
        "command": "terraform.agent.showMemory",
        "title": "Agent: Show Memory",
        "icon": "$(notebook)",
        "category": "Terraform"
      },
      {
        "command": "terraform.agent.learnNow",
        "title": "Agent: Learn From Repos Now",
        "icon": "$(cloud-download)",
        "category": "Terraform"
      },
      {
        "command": "terraform.diagnoseAuth",
        "title": "Diagnose GitHub Auth Scopes",
        "icon": "$(verified)",
        "category": "Terraform"
      },
      {
        "command": "terraform.resolveVariable",
        "title": "Resolve Variable Source",
        "icon": "$(search)",
        "category": "Terraform"
      },
      {
        "command": "terraform.reviewDeployment",
        "title": "Review Deployment",
        "icon": "$(thumbsup)",
        "category": "Terraform"
      },
      {
        "command": "terraform.callNotes.open",
        "title": "Open Call Notes",
        "icon": "$(notebook)",
        "category": "Terraform"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "terraform.callNotes.open",
          "when": "editorTextFocus"
        }
      ],
      "view/title": [
        {
          "command": "terraform.refreshWorkspaces",
          "when": "view == terraform.workspaces",
          "group": "navigation"
        },
        {
          "command": "terraform.bootstrapWorkspace",
          "when": "view == terraform.workspaces",
          "group": "navigation"
        },
        {
          "command": "terraform.configureWorkspace",
          "when": "view == terraform.workspaces",
          "group": "navigation"
        },
        {
          "command": "terraform.syncWorkflows",
          "when": "view == terraform.workspaces",
          "group": "navigation"
        },
        {
          "command": "terraform.addVariable",
          "when": "view == terraform.variables",
          "group": "navigation"
        },
        {
          "command": "terraform.addSecret",
          "when": "view == terraform.variables",
          "group": "navigation"
        },
        {
          "command": "terraform.requiredSetup.setAll",
          "when": "view == terraform.requiredSetup",
          "group": "navigation"
        },
        {
          "command": "terraform.requiredSetup.refresh",
          "when": "view == terraform.requiredSetup",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "terraform.selectWorkspace",
          "when": "viewItem == workspace",
          "group": "inline"
        },
        {
          "command": "terraform.runPlan",
          "when": "viewItem == workspace",
          "group": "inline"
        },
        {
          "command": "terraform.runApply",
          "when": "viewItem == workspace",
          "group": "inline"
        },
        {
          "command": "terraform.openRunLogs",
          "when": "viewItem == run",
          "group": "inline"
        },
        {
          "command": "terraform.deleteVariable",
          "when": "viewItem == variable",
          "group": "inline"
        },
        {
          "command": "terraform.openInGitHub",
          "when": "viewItem =~ /workspace|run/",
          "group": "context"
        }
      ]
    },
    "configuration": {
      "title": "Terraform Workspace",
      "properties": {
        "terraformWorkspace.repoOrg": {
          "type": "string",
          "description": "Default GitHub organization for new workspaces",
          "default": "HappyPathway"
        },
        "terraformWorkspace.agent.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable the autonomous agent. Polls GitHub Issues for the configured trigger label and attempts to resolve them via the registered Terraform tools."
        },
        "terraformWorkspace.agent.owners": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "GitHub orgs/users whose issue queues the agent should scan."
        },
        "terraformWorkspace.agent.triggerLabel": {
          "type": "string",
          "default": "agent",
          "description": "Issue label that flags an issue for the autonomous agent."
        },
        "terraformWorkspace.agent.pollIntervalMinutes": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "description": "How often the agent wakes itself up to scan the queue."
        },
        "terraformWorkspace.agent.runOnFocus": {
          "type": "boolean",
          "default": true,
          "description": "Also wake the agent when VS Code regains focus."
        },
        "terraformWorkspace.agent.maxTasksPerTick": {
          "type": "number",
          "default": 1,
          "minimum": 1,
          "description": "Maximum tasks the agent will attempt in a single wake-up."
        },
        "terraformWorkspace.agent.maxIterations": {
          "type": "number",
          "default": 12,
          "minimum": 1,
          "description": "Hard ceiling on tool-call iterations per task to prevent runaway loops."
        },
        "terraformWorkspace.agent.autonomyLevel": {
          "type": "string",
          "enum": [
            "observe",
            "draft-pr",
            "merge"
          ],
          "default": "draft-pr",
          "description": "How autonomously the agent acts. `observe` only records findings; `draft-pr` opens PRs for review; `merge` merges when confident."
        },
        "terraformWorkspace.agent.learner.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Continuously ingest knowledge from repos topic-tagged with the configured learning topic. Runs at the start of every agent tick."
        },
        "terraformWorkspace.agent.learner.owners": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "HappyPathway",
            "djaboxx"
          ],
          "description": "GitHub orgs/users to scan for learning-tagged repos."
        },
        "terraformWorkspace.agent.learner.topic": {
          "type": "string",
          "default": "learning",
          "description": "GitHub repo topic that opts a repo into continuous knowledge ingestion."
        },
        "terraformWorkspace.agent.learner.reposPerTick": {
          "type": "number",
          "default": 25,
          "minimum": 1,
          "description": "Maximum repos the learner ingests per tick (across all owners)."
        },
        "terraformWorkspace.agent.learner.commitsPerRepo": {
          "type": "number",
          "default": 20,
          "minimum": 1,
          "description": "Maximum recent commits inspected per repo per tick."
        },
        "terraformWorkspace.auth.enableOidc": {
          "type": "boolean",
          "default": true,
          "description": "Assume the org supports GitHub Actions OIDC (`id-token: write`). If false the extension will offer PAT/GitHub App guidance when scaffolding trust policies."
        },
        "terraformWorkspace.auth.preferredAuthMethod": {
          "type": "string",
          "enum": [
            "oidc",
            "github-app",
            "personal-access-token"
          ],
          "default": "oidc",
          "description": "Preferred authentication method when OIDC is unavailable. Only used for guidance; the extension does not auto-configure provider-specific infra."
        },
        "terraformWorkspace.compositeActionOrg": {
          "type": "string",
          "description": "GitHub org containing composite action repos (aws-auth, gh-auth, terraform-init, etc.)",
          "default": "HappyPathway"
        },
        "terraformWorkspace.defaultRunnerGroup": {
          "type": "string",
          "description": "Default runner group label for GitHub Actions",
          "default": "self-hosted"
        },
        "terraformWorkspace.defaultStateRegion": {
          "type": "string",
          "description": "Default AWS region for Terraform state S3 buckets",
          "default": "us-east-1"
        },
        "terraformWorkspace.preferOpenTofu": {
          "type": "boolean",
          "description": "Prefer OpenTofu (tofu) over Terraform CLI in generated workflows",
          "default": true
        },
        "terraformWorkspace.useLocalActions": {
          "type": "boolean",
          "description": "Scaffold composite actions into the repo at .github/actions/* and reference them locally instead of pulling from an external GitHub org. Recommended.",
          "default": true
        },
        "terraformWorkspace.autoSyncWorkflows": {
          "type": "boolean",
          "description": "Automatically regenerate workflows when .vscode/terraform-workspace.json changes.",
          "default": true
        },
        "terraformWorkspace.aiModel": {
          "type": "string",
          "description": "Language model family to use for code generation",
          "default": "gpt-4o"
        },
        "terraformWorkspace.driftCheckMinutes": {
          "type": "number",
          "description": "Minutes between automatic drift checks (0 disables the timer; the `Terraform: Check Drift` command still works on demand).",
          "default": 0,
          "minimum": 0
        },
        "terraformWorkspace.enableTelemetry": {
          "type": "boolean",
          "description": "Send anonymous usage telemetry. Off by default; respects VS Code's global telemetry setting.",
          "default": false
        }
      }
    },
    "configurationDefaults": {
      "[terraform]": {
        "editor.tabSize": 2,
        "editor.insertSpaces": true,
        "editor.formatOnSave": true,
        "editor.detectIndentation": false
      },
      "[terraform-vars]": {
        "editor.tabSize": 2,
        "editor.insertSpaces": true
      }
    },
    "walkthroughs": [
      {
        "id": "terraform-workspace.getStarted",
        "title": "Get Started with Terraform Workspace",
        "description": "Bootstrap a Terraform repo from a template, then drive plans/applies through GitHub Actions with AI in the loop.",
        "featuredFor": [
          "**/*.tf",
          "**/.github/workflows/terraform-*.yml",
          "**/.vscode/terraform-workspace.json"
        ],
        "steps": [
          {
            "id": "scaffold",
            "title": "Scaffold a new repo from a template",
            "description": "Start here if you don't have a repo open yet. Generate a new GitHub repository from a template — no folder required.\n[Scaffold From Template](command:terraform.scaffoldFromTemplate)",
            "media": {
              "markdown": "media/walkthrough/scaffold.md"
            }
          },
          {
            "id": "auth",
            "title": "Sign in & diagnose GitHub scopes",
            "description": "This extension talks to GitHub for everything. Confirm your token has `repo`, `read:org`, and `workflow`.\n[Diagnose GitHub Auth Scopes](command:terraform.diagnoseAuth)",
            "media": {
              "markdown": "media/walkthrough/auth.md"
            }
          },
          {
            "id": "configure",
            "title": "Configure your workspace",
            "description": "Open the configuration panel to fill in repo, environments, and backend settings.\n[Configure Workspace](command:terraform.configureWorkspace)",
            "media": {
              "markdown": "media/walkthrough/configure.md"
            }
          },
          {
            "id": "oidc",
            "title": "Wire OIDC trust & remote backend",
            "description": "Generate the AWS IAM trust policy and the S3+DynamoDB backend HCL. No long-lived AWS keys.\n[Scaffold OIDC Trust](command:terraform.scaffoldOidcTrust)\n[Scaffold Backend](command:terraform.scaffoldBackend)",
            "media": {
              "markdown": "media/walkthrough/oidc.md"
            }
          },
          {
            "id": "sync",
            "title": "Sync workflows",
            "description": "Generate `.github/workflows/terraform-plan-*.yml` and `terraform-apply-*.yml` for every environment.\n[Sync Workflows](command:terraform.syncWorkflows)",
            "media": {
              "markdown": "media/walkthrough/sync.md"
            }
          },
          {
            "id": "lint",
            "title": "Lint generated workflows",
            "description": "Run `actionlint` over generated workflows to catch syntax issues before pushing.\n[Lint Workflows](command:terraform.lintWorkflows)",
            "media": {
              "markdown": "media/walkthrough/lint.md"
            }
          },
          {
            "id": "chat",
            "title": "Operate via the chat participant",
            "description": "Plans, variable resolution, drift checks, org-wide HCL search — all through `@terraform`.\n[Open Chat](command:workbench.action.chat.open?%7B%22query%22%3A%22%40terraform+%22%7D)",
            "media": {
              "markdown": "media/walkthrough/chat.md"
            }
          },
          {
            "id": "drift",
            "title": "Check for drift",
            "description": "Manually trigger a drift check across environments.\n[Check Drift](command:terraform.checkDrift)",
            "media": {
              "markdown": "media/walkthrough/drift.md"
            }
          }
        ]
      }
    ]
  },
  "scripts": {
    "help": "node scripts/help.js",
    "vscode:prepublish": "npm run build",
    "build": "node esbuild.js --production",
    "watch": "node esbuild.js --watch",
    "compile": "tsc --noEmit",
    "lint": "eslint src --ext ts",
    "lint:fix": "eslint src --ext ts --fix",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "test": "vitest run",
    "test:unit": "vitest run test/unit",
    "test:integration": "vitest run test/integration",
    "test:watch": "vitest",
    "test:vscode": "tsc -p test/vscode/tsconfig.json && node out/test-vscode/runTest.js",
    "coverage": "vitest run --coverage",
    "package": "npx --yes @vscode/vsce package --no-dependencies",
    "install-latest": "bash scripts/install-latest.sh",
    "install-pre": "bash scripts/install-latest.sh --pre",
    "clean": "node -e \"const fs=require('fs');for(const p of ['dist','coverage','.vitest-cache']){try{fs.rmSync(p,{recursive:true,force:true})}catch{}};for(const f of fs.readdirSync('.')){if(f.endsWith('.vsix'))fs.rmSync(f)}\""
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.95.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vitest/coverage-v8": "^4.1.5",
    "@vscode/test-electron": "^2.3.9",
    "esbuild": "^0.20.0",
    "eslint": "^8.57.0",
    "mocha": "^11.7.5",
    "typescript": "^5.4.0",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "@types/libsodium-wrappers": "^0.7.14",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "libsodium-wrappers": "^0.8.4"
  }
}

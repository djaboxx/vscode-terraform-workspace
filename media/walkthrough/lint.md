# Lint workflows

`Terraform: Lint Workflows` runs [actionlint](https://github.com/rhysd/actionlint) over the generated `.github/workflows/*.yml` files and surfaces results as Problems-panel diagnostics.

Install actionlint with Homebrew (`brew install actionlint`), `go install`, or grab a release binary.
